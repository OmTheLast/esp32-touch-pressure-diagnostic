#pragma once

// Copy this file to include/secrets.h and enter your own Wi-Fi credentials.
// include/secrets.h is ignored by Git and must never be committed.
constexpr char WIFI_NAME[] = "YOUR_WIFI_NAME";
constexpr char WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";
